# Public Behavior Fixtures

This directory contains pre-refactor public output fixtures for CLI commands.

## Commands Covered

- `doctor` - text output and exit semantics
- `analyze` - success/failure semantics
- `query` - output contracts
- `serve`/dashboard - response shapes
- `rpc` - JSON response shapes

## Usage

Fixtures are loaded by compatibility tests and compared with refactored output.
